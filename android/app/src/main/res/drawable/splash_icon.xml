<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Using negative insets to slightly enlarge the logo and push edge artifacts
         outside of the system's circular mask -->
    <item
        android:drawable="@mipmap/sleepr_aid_foreground"
        android:left="-6dp"
        android:top="-6dp"
        android:right="-6dp"
        android:bottom="-6dp" />
</layer-list>